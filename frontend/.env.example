# VITE_BACKEND_URL
# Optional. In development, Vite proxies /api to the backend (see vite.config.ts).
# In production, set this to the backend URL (e.g., https://api.example.com).
# Leave empty to use same-origin (proxy handles /api routes).
VITE_BACKEND_URL=http://localhost:8000
